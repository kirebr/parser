plugins {
  id "org.sonarqube" version "3.2.0"
}

sonarqube {
  properties {
    property "sonar.projectKey", "kirebr_parser"
    property "sonar.organization", "kirebr"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
